# Overview
JacobHQ proxy is a way to have all parts of the JacobHQ API put into your Next.js app. Whilst with JacobHQ UI public use is encouraged, Proxy is intended for internal JHQ app use. Proxy is currently in the concept phase.